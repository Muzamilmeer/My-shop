
import React from "react";
import ReactDOM from "react-dom/client";

const App = () => <h1 className="text-accent text-3xl font-bold">Welcome to STREETFLOW</h1>;

ReactDOM.createRoot(document.getElementById("root")!).render(<App />);
